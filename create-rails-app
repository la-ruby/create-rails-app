#!/bin/bash


##
# runs rails new ...
#
install_rails() {
  git clone -b 7-0-stable --single-branch https://github.com/rails/rails /tmp/rails
  cd /tmp/rails
  bundle exec railties/exe/rails new /tmp/created-rails-app --dev --database=postgresql
}

main() {
  install_rails
}

main
