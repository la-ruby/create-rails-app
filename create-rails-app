#!/bin/bash -x

##
# Globals
#
DIR=$(dirname "$0")         # where this script is
TARGET="6-0-stable"         # target rails version

##
# Install edge rails
#
cd $DIR
time git clone -b $TARGET --single-branch https://github.com/rails/rails
